import { FontFormatPipe } from './font-format.pipe';

describe('FontFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new FontFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
